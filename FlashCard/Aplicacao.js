// Não há uma tradução direta de pacotes em JavaScript, então podemos omitir o pacote src
// Supondo que GUI seja uma classe ou objeto existente em JavaScript

class Aplicacao {
    static main(args) {
        new GUI();
    }
}
